@page
@model Market.Pages.Groups.CreateModel
@{
    ViewData["Title"] = "Create";
}

<h2>Create Group</h2>

<div class="container">
    @*<form method="post">
        <label asp-for="@Model.NewGroup.Id">ID: </label>
        <input asp-for="@Model.NewGroup.Id" />
        <br />
        <label asp-for="@Model.NewGroup.GroupName">Group Name: </label>
        <input asp-for="@Model.NewGroup.GroupName" />
        <br />
        <input type="submit" class="btn btn-info" />
        <a class="btn btn-outline-info" asp-page="/Groups/ShowGroups">Cancle</a>
    </form>*@
    <form method="post">
        @*<div class="form-group">*@
            @*<label asp-for="@Model.NewGroup.Id">ID</label>*@
            @*<input type="text" class="form-control" asp-for="@Model.NewGroup.Id" placeholder="Enter Id">*@
        @*</div>*@
        <div class="form-group">
            <label asp-for="@Model.NewGroup.GroupName">Group name</label>
            <input type="text" class="form-control" asp-for="@Model.NewGroup.GroupName"  placeholder="Enter name">
        </div>


        <button type="submit" class="btn btn-primary">Submit</button>
        <a class="btn btn-outline-info" asp-page="/Groups/ShowGroups">Cancle</a>
    </form>
</div>
